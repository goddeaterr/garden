// Static trees removed — all trees now come from the live marketplace scraper.
// See lib/useTrees.ts for the dynamic hook.
import type { Tree } from '@/types';
export const trees: Tree[] = [];
export const featuredTrees: Tree[] = [];
